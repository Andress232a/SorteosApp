# Archivo de ejemplo de variables de entorno para cPanel
# Copia este contenido y crea un archivo .env en el servidor con estos valores

# Tipo de base de datos (mysql para cPanel)
DB_TYPE=mysql

# Configuración de MySQL (obtén estos valores de cPanel)
DB_HOST=localhost
DB_PORT=3306
DB_USER=premioclick_user
DB_PASSWORD=tu_password_aqui
DB_NAME=premioclick_db
DB_SSL=false

# JWT Secret (cambia esto por una clave segura)
JWT_SECRET=premioclick_jwt_secret_2025_super_seguro_cambiar_en_produccion

# PayPal (si usas PayPal)
PAYPAL_CLIENT_ID=tu_paypal_client_id
PAYPAL_CLIENT_SECRET=tu_paypal_client_secret
PAYPAL_MODE=sandbox

# Puerto del servidor (ajusta según tu configuración de Node.js en cPanel)
PORT=3001
HOST=0.0.0.0

# URL de la API (ajusta según tu dominio)
API_URL=https://premioclick.cl/api
